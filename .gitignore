# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets & local state
.env
.env.*
!.env.example
# hosted-agents.json holds raw private keys for hosted persona agents — never commit.
server/*.json
!server/package.json
!server/package-lock.json

# Hardhat
contracts/artifacts
contracts/cache
contracts/typechain-types

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local index/SQLite state written by the runtime and by `npm --prefix server test`
# (server/indexer.js falls back to ./.state when no /data volume is mounted).
.state/

# Railway local link
.railway
.vercel
.env*

# Python / GenLayer compiler artifacts
__pycache__/
*.py[cod]
