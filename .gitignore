# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets & local state
.env
.env.*
!.env.example
# hosted-agents.json holds raw private keys for hosted persona agents — never commit.
server/*.json
!server/package.json
!server/package-lock.json

# Hardhat
contracts/artifacts
contracts/cache
contracts/typechain-types

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Railway local link
.railway
.vercel
.env*

# Local SQLite index state (rebuilt from chain; lives on the volume in production)
*.db
*.db-shm
*.db-wal
.state/
